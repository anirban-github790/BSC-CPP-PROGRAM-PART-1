#include<iostream>
/*using namespace std;
class sample
{
	private:
		int arr[5];
	public:
		sample(int *a)
		{
			int i;
			for(i=0;i<5;i++)
			{
				arr[i]=a[i];
			}
		}
		int operator[](int n)
		{
			if(n<0 || n>5)
			{
				cout<<"Array out of bound";
				exit(0);
			}
			return(arr[n]);
		}
};
int main()
{
	int x[5]={1,2,3,4,5};
	sample s(x);
	for(int i=0;i<5;i++)
	{
		cout<<x[i]<<"\n";
	}
	return 0;
}*/
int main()
{
	char ch=')';
	printf("%d",ch);
}
